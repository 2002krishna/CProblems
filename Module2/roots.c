#include<stdio.h>
#include<math.h>
int main()
{
  int r,r1,r2,a,b;
  scanf("%d %d", &a,&b);
if(r>0)
{
r1=-b+sqrt(r)/2*a;
r2=-b-sqrt(r)/2*a;

    printf("The two roots are %d %d", r1,r2);
}
else;
{
   printf("No roots");
}
}
